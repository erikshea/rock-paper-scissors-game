package rps;

import static org.junit.Assert.*;

import org.junit.Test;

public class RockPaperScissorsGameTest extends RPSGame {
/*
	@Test
	public void testPlayRound() {
		assertEquals("Match nul",playRound(1,1));
		assertEquals("Match nul",playRound(2,2));
		assertEquals("Match nul",playRound(3,3));
		assertEquals(playerScore,0);
		assertEquals(computerScore,0);
		
		assertEquals("Gagné",playRound(1,3));
		assertEquals("Gagné",playRound(2,1));
		assertEquals("Gagné",playRound(3,2));
		assertEquals(playerScore,3);
		assertEquals(computerScore,0);
		
		assertEquals("Perdu",playRound(3,1));
		assertEquals("Perdu",playRound(1,2));
		assertEquals("Perdu",playRound(2,3));
		assertEquals(playerScore,3);
		assertEquals(computerScore,3);
	}
*/
}
